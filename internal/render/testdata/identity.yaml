# Rendered by otlp-collector-oidc run from the environment.
# The variables are documented in docs/configuration.md.

receivers:
  otlpsingleport:
    endpoint: "0.0.0.0:4318"
    max_request_body_size: 4194304
    tls:
      cert_file: "/etc/otlp-collector-oidc/tls/cert.pem"
      key_file: "/etc/otlp-collector-oidc/tls/key.pem"
      reload_interval: "1m0s"
    auth:
      authenticator: oidcclientauth

processors:
  memory_limiter:
    check_interval: 1s
    limit_mib: 64
    spike_limit_mib: 16
  # Identity from the token, on every span and log record (identity.go).
  attributes/identity:
    actions:
      - key: "enduser.id"
        action: delete
      - key: "enduser.id"
        action: upsert
        from_context: "auth.enduser.id"
      - key: "enduser.groups"
        action: delete
      - key: "enduser.groups"
        action: upsert
        from_context: "auth.enduser.groups"
  # The deployment's static attributes, on every client resource.
  resource/deployment:
    attributes:
      - key: "deployment.environment.name"
        value: "prod"
        action: upsert
      - key: "telemetry_source"
        value: "client"
        action: upsert
  batch:
    timeout: "5s"

exporters:
  otlp_grpc:
    endpoint: "collector:4317"
    tls:
      insecure: true
    timeout: "10s"
    compression: "gzip"
    sending_queue:
      queue_size: 1000
    retry_on_failure:
      enabled: true
      initial_interval: "5s"
      max_elapsed_time: "1m0s"
      max_interval: "30s"

extensions:
  health_check:
    endpoint: "127.0.0.1:13133"
  oidcclientauth:
    issuer_url: "https://idp.example.com/"
    audience: "telemetry"
    discovery_retry: "30s"
    jwks_refresh: "10m0s"
    required_scope: "telemetry:write"
    required_claims: ["sub", "preferred_username"]
    claim_attributes:
      "sub": "enduser.id"
      "groups": "enduser.groups"
    clock_skew: "1m0s"
    rejection_log_interval: "1m0s"

service:
  extensions: [health_check, oidcclientauth]
  telemetry:
    # The process's own identity, on its logs and on target_info at :8888.
    # service.version is the build's, which the collector adds itself.
    resource:
      "service.name": "otlp-collector-oidc"
    logs:
      level: "info"
      encoding: "json"
      output_paths: ["stdout"]
      error_output_paths: ["stderr"]
      # Own logs to the logs upstream (LOG_OUTPUT), as the SDK's schema has it.
      processors: [{"batch":{"exporter":{"otlp":{"protocol":"grpc","endpoint":"http://collector:4317","insecure":true,"compression":"gzip","timeout":10000}}}}]
    metrics:
      readers:
        - pull:
            exporter:
              prometheus:
                host: "0.0.0.0"
                port: 8888
  pipelines:
    traces:
      receivers: [otlpsingleport]
      processors: [memory_limiter, attributes/identity, resource/deployment, batch]
      exporters: [otlp_grpc]
    logs:
      receivers: [otlpsingleport]
      processors: [memory_limiter, attributes/identity, resource/deployment, batch]
      exporters: [otlp_grpc]
